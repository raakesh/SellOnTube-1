# build output
dist/
.output/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

pnpm-lock.yaml

.astro

# SEO data pipeline — never commit credentials or raw data
scripts/credentials.json
scripts/seo_data/*.json
# Keyword research — ignore binary Excel files, but commit CSVs (plain text, diffable)
research/**/*.xlsx
research/**/*.xls

# Claude Code local settings — contains GitHub tokens and machine-specific paths
.claude/
